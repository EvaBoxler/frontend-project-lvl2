import { NormalizedConfig, Snapshot, Trigger } from './definitions';
export declare class Runtime {
    private _data_map;
    private _filename;
    private _config;
    constructor(filename: string, config: NormalizedConfig, triggers: Trigger[], snapshots: Snapshot[]);
    get_type_inference_or_diagnostic(body_line: number): string;
    get_type_inference_or_throw_diagnostic(body_line: number): string;
    get_type_report(body_line: number): string;
    get_value_report(body_line: number, getter: () => any): string;
    private _create_report;
}
