import * as JestTransform from '@jest/transform';
import { JestConfig } from './definitions';
export declare const transform: (source_text: string, source_filename: string, { config: jest_config }: {
    config: JestConfig;
}) => ReturnType<JestTransform.SyncTransformer['process']>;
