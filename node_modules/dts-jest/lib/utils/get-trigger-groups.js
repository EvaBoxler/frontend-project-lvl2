"use strict";
exports.__esModule = true;
exports.get_trigger_groups = void 0;
var get_trigger_groups = function (headers) {
    var group_set = new Set();
    headers.forEach(function (header) {
        if (header.group !== undefined) {
            group_set.add(header.group);
        }
    });
    return Array.from(group_set);
};
exports.get_trigger_groups = get_trigger_groups;
