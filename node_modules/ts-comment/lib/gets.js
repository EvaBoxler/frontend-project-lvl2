"use strict";
exports.__esModule = true;
var _ts = require("typescript");
var for_each_1 = require("./for-each");
function gets(source, callback, 
    // istanbul ignore next
    ts) {
    if (callback === void 0) { callback = function (comment) { return comment; }; }
    // istanbul ignore next
    if (ts === void 0) { ts = _ts; }
    var results = [];
    for_each_1.for_each(source, function (comment, scanner, source_file) {
        var result = callback(comment, scanner, source_file);
        results.push(result);
    }, ts);
    return results;
}
exports.gets = gets;
